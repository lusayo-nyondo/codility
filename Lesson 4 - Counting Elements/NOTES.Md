So what is a frequency array?

An array that records the counts of numbers at a given index.

If you know that an array contains elements in the range 0 - N, then the frequency array should be of size N + 1 (so it can sufficiently record all numbers).